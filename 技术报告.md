# Qwen-Ascend-LLM 技术报告

> 基于 PyTorch → ONNX → OM 技术路线，在昇腾（Ascend）边缘设备上部署大语言模型的完整工程实践

---

## 目录

1. [项目概述](#1-项目概述)
2. [为什么选择 PyTorch → ONNX → OM 技术路线](#2-为什么选择-pytorch--onnx--om-技术路线)
3. [ONNX 与 OM 格式的优缺点对比](#3-onnx-与-om-格式的优缺点对比)
4. [技术细节一：如何魔改 Qwen2 模型结构](#4-技术细节一如何魔改-qwen2-模型结构)
5. [技术细节二：PyTorch → ONNX 的转换](#5-技术细节二pytorch--onnx-的转换)
6. [技术细节三：为什么以及如何修改导出的 ONNX](#6-技术细节三为什么以及如何修改导出的-onnx)
7. [技术细节四：ONNX → OM 的编译](#7-技术细节四onnx--om-的编译)
8. [运行时推理：ACL 执行引擎](#8-运行时推理acl-执行引擎)
9. [关键参数约束](#9-关键参数约束)
10. [迁移到其他模型（Qwen3 / DeepSeek）的通用性分析](#10-迁移到其他模型qwen3--deepseek的通用性分析)
11. [总结](#11-总结)

---

## 1. 项目概述

`qwen-ascend-llm` 是一个**将 Qwen1.5 / Qwen2 系列大语言模型部署到华为昇腾（Ascend）NPU 边缘设备上运行的完整工程**，参考自 [ascend-llm](https://gitee.com/yinghuo302/ascend-llm) 项目。核心目标是：让一个原本用 PyTorch + HuggingFace 训练/发布的通用大模型，能在算力和内存都受限的昇腾边缘芯片（主要测试平台是 **Ascend 310B1**，例如香橙派 AIPro 20T 版）上以可接受的延迟运行，并对外提供服务。

### 1.1 核心能力

项目主要覆盖三件事：

1. **模型格式转换流水线**：`PyTorch(HuggingFace) → ONNX → OM`，把模型编译成昇腾专用的离线模型格式。
2. **多后端推理引擎**：同一套推理管线支持三种运行后端（由 `session_type` 控制）：
   - `pytorch`：CPU/NPU 上跑原始 PyTorch 模型 —— 用于**验证魔改后的模型结构是否正确**。
   - `onnx`：ONNX Runtime 在 CPU 上运行 —— 用于**验证/调试**，不依赖昇腾环境也能对话。
   - `acl`：昇腾 NPU 上跑编译好的 OM 模型 —— **生产部署路径**。
3. **服务化能力**：交互式命令行对话（`cli_chat.py`）、兼容 OpenAI 协议的 API 服务（`api.py`，支持流式输出、function call），以及 Docker 一键部署。

### 1.2 功能清单

- [x] 导出 ONNX、OM 模型
- [x] 模型推理，支持 ONNX 推理（仅 CPU）
- [x] 模型推理，支持 CANN（NPU）推理
- [x] CANN 推理时使用动态 shape 推理以降低首字延迟
- [x] 流式传输
- [x] 兼容 OpenAI 的 API 搭建
- [x] 支持 function call
- [x] 支持 Docker 快速部署
- [ ] 支持模型量化（weight-only、smooth quant 等）—— TODO

### 1.3 代码组织

| 模块 | 文件 | 职责 |
|---|---|---|
| **入口** | `api.py` / `cli_chat.py` / `main.py` | API 服务 / 命令行对话 / 最小入口 |
| **导出流水线** | `export/modeling_qwen2.py` | 魔改版 Qwen2 架构（基于 transformers v4.37.0） |
| | `export/export_onnx.py` | PyTorch → ONNX 转换 |
| | `export/change_node.py` | ONNX 图手术（修复 Trilu 节点） |
| | `export/onnx2om.py` | ONNX → OM 编译（通过 ATC） |
| | `export/compare.py` | 逐层输出对比（ONNX vs OM） |
| **运行时** | `utils/session.py` | 会话抽象（`OnnxSession` / `PyTorchSession` / `AclSession`） |
| | `utils/inference.py` | 统一推理管线与采样逻辑 |
| | `utils/kvcache.py` | KV-cache 管理策略 |
| | `utils/engine.py` | 底层 ACL 运行时封装 |
| **配置** | `config.py` | `InferenceConfig`，集中所有运行参数 |

---

## 2. 为什么选择 PyTorch → ONNX → OM 技术路线

边缘设备部署大模型的核心矛盾是：**模型是 PyTorch 生态产出的，但昇腾 NPU 的高效执行依赖它自己的图编译器和算子库**。中间需要一个"通用可移植的中间表示"来解耦这两端，于是形成三段式路线，每段各司其职。

### 2.1 PyTorch 端：模型的来源，但不适合直接部署

- HuggingFace 上的 Qwen 权重、模型结构都是 PyTorch 定义的，训练/微调生态最成熟。
- 但 PyTorch 是**动态图 + 解释执行**，运行时依赖庞大（Python + libtorch），对昇腾 NPU 的原生支持有限（虽有 `torch_npu`，但难以做深度图优化）。直接在边缘设备上跑既重又慢。
- 因此 PyTorch 在本项目中主要承担**权重来源**和**结构正确性验证基准**（`session_type=pytorch`，CPU fp32 跑通《出师表》背诵等 demo）。

### 2.2 ONNX 端：跨框架的"通用中间表示"（桥梁）

- ONNX 是厂商中立的标准算子图格式。PyTorch 原生支持 `torch.onnx.export`，昇腾 ATC 编译器也把 ONNX 作为主要输入框架之一（`--framework=5` 即代表 ONNX）。
- 它把模型从"Python 代码 + 动态图"固化为**一张静态的、与框架无关的计算图**，作为 PyTorch 与昇腾 OM 之间的**解耦桥梁**。没有这个中间层，就得为昇腾单独手写模型定义。
- ONNX 还能用 ONNX Runtime 在**纯 CPU** 上运行，天然成为一个"无昇腾环境也能验证"的黄金参照物——当 OM 输出异常时，可逐层对比 ONNX 与 OM 找出溢出层（项目用 MSIT 工具做这件事）。

### 2.3 OM 端：昇腾专用离线模型（真正的部署目标）

- OM（Offline Model）是昇腾 CANN 通过 **ATC（Ascend Tensor Compiler）** 把 ONNX 编译后得到的**离线模型**。它已完成针对具体芯片（`soc_version`，如 Ascend310B1）的算子选择、图融合、内存规划、精度模式设定等编译期优化。
- 运行时通过 **ACL（Ascend Computing Language）** 接口加载执行，无需再依赖 PyTorch/ONNX Runtime，启动和推理都由昇腾运行时接管，能充分发挥 NPU 的 AI Core 算力。
- 这是**边缘部署的最终产物**：一次编译，反复加载运行。

> **一句话总结**：PyTorch 负责"产出模型"，ONNX 负责"标准化与验证"，OM 负责"在昇腾芯片上高效执行"。三段解耦，使同一个模型能在训练生态、调试环境、生产 NPU 三处流转，每步都有独立可验证的产物。

---

## 3. ONNX 与 OM 格式的优缺点对比

### 3.1 ONNX 格式

**优点**

- **跨平台/跨框架通用**：厂商中立标准，PyTorch/TF/Paddle 都能导出，各种推理引擎都能读。
- **可移植、可调试**：可用 ONNX Runtime 在纯 CPU 上运行，不依赖专用硬件，便于在无 NPU 的机器上验证对话逻辑。
- **图可编辑**：结构透明，可用 `onnx` Python API 直接做"图手术"修改节点（`change_node.py`）。
- **支持动态 shape**：`dynamic_axes` 天然支持 batch/seq 维度动态，对 LLM 变长输入友好。

**缺点**

- **未针对特定硬件优化**：只是通用图，没有芯片级算子融合、内存排布优化，直接跑效率不如专用格式。
- **CPU 推理慢**：本项目 ONNX 只支持 CPU，1.5B 模型速度较慢，仅适合验证而非生产。
- **算子兼容性问题**：某些 ONNX 算子昇腾 ATC 无法识别，需手动改图（见 Trilu 问题）。
- 权重大（本项目用 fp16 导出并借助 `save_as_external_data` 外置权重）。

### 3.2 OM 格式

**优点**

- **深度硬件优化**：ATC 编译期完成算子融合、AI Core 算子映射、内存规划，充分发挥昇腾 NPU 性能。
- **部署轻量**：运行时只依赖 ACL/CANN，不需要 PyTorch/ONNX Runtime，适合边缘设备。
- **加载快、可复用**：一次编译，离线保存，反复加载。项目还把模型分块读进 host 内存再 `load_from_mem` 优化加载。
- **支持动态分档 shape**：通过 `--dynamic_dims` 编译多个 shape 档位，兼顾变长输入与静态编译效率。
- **精度模式可控**：可指定 `mixed_float16`、`origin` 等精度模式，并用 `modify_mixlist`（`ops_info.json`）对个别算子做精度覆盖。

**缺点**

- **强绑定昇腾硬件**：OM 与 `soc_version` 芯片型号强耦合，换芯片要重新编译，完全不可移植。
- **编译耗时、体积膨胀**：开启动态 shape 后模型体积增长 50%–100%，编译时间随 `max_prefill_length` 增大而变长，推理内存占用也上升。
- **黑盒难调试**：编译后是黑盒，出问题（尤其 fp16 溢出）难以定位，必须借助 MSIT 工具逐层与 ONNX 对比。
- **参数约束严格**：`kv_cache_length`、`max_prefill_length` 等必须与导出时严格一致，否则转换或推理失败。

### 3.3 对比小结

| 维度 | ONNX | OM |
|---|---|---|
| 可移植性 | 强（跨框架跨硬件） | 无（绑定昇腾芯片型号） |
| 运行硬件 | CPU（本项目） | 昇腾 NPU |
| 性能 | 一般，未硬件优化 | 高，编译期深度优化 |
| 调试友好度 | 高，可编辑可对比 | 低，黑盒 |
| 定位 | 验证/调试的桥梁 | 生产部署产物 |

---

## 4. 技术细节一：如何魔改 Qwen2 模型结构

> **核心结论**：数学上的模型架构（层数、注意力机制、权重、计算逻辑）**没有改变**，改的是"计算图的形态"和"输入输出接口"，目的是让它能被**静态导出**成 ONNX/OM。权重与原始 HuggingFace checkpoint 完全兼容。

改造集中在 `export/modeling_qwen2.py`（基于 transformers v4.37.0 原版魔改），属于**源码级的图工程改造（graph surgery at source level）**，而非架构创新。主要有五类改动：

### 4.1 接口签名简化 —— 从"灵活 HF 接口"到"固定四输入两输出"

原版 `forward` 有十几个可选参数（`use_cache`、`inputs_embeds`、`output_attentions`、`return_dict`…）。魔改版全部注释掉，`Qwen2ForCausalLM.forward` 只保留 4 个输入：

```python
def forward(self, input_ids, attention_mask, position_ids, past_key_values):
    ...
```

输出固定为 `(logits, out_key_values)`。这是因为 ONNX 导出要求**图的输入输出是固定张量**，不能有 Python 分支、可选参数或字典返回。

### 4.2 KV-cache：从"动态 tuple 列表"到"单个固定形状大张量"（最核心改动）

- **原版 HF**：使用 `DynamicCache` / 每层一对 `(key, value)` 的 tuple 列表，长度随生成动态增长。
- **魔改版**：把整个 KV-cache 压成**一个四维张量**，形状为：

  ```
  (batch, kv_cache_length, num_layers * 2 * num_kv_heads, per_head_dim)
  ```

- 每层 attention 用**切片**从大张量取出本层的 key/value（`modeling_qwen2.py:283-292`）：

  ```python
  cache_key = past_key_value[
      :,
      self.layer_idx * 2 * self.num_key_value_heads : (self.layer_idx * 2 + 1) * self.num_key_value_heads
  ]
  cache_value = past_key_value[
      :,
      (self.layer_idx * 2 + 1) * self.num_key_value_heads : (self.layer_idx * 2 + 2) * self.num_key_value_heads
  ]
  key_states = torch.cat((cache_key, key_states), dim=2)
  value_states = torch.cat((cache_value, value_states), dim=2)
  ```

- 在 `Qwen2Model.forward` 里用 `transpose` + `concat` 把各层输出的 KV 重新拼回同一形状（`:1103, :1172-1176`）。

**这样做是因为静态图要求 KV-cache 的形状在编译期固定，不能动态增长。** 这也是 `kv_cache_length` 上限（等价于上下文窗口长度）的来源。

### 4.3 attention_mask 构造搬进图内，用固定值代替 `finfo.min`

- 原版调用 `_prepare_4d_causal_attention_mask`；魔改版全部注释掉，改用自写的静态 `get_masks`（`:973-1005`），用 `torch.ones().tril_()` 构造下三角因果掩码，再 `cat` 上历史部分。
- mask 填充值从 `torch.finfo(dtype).min`（fp16 下约 -65504）**改成硬编码的 `-10000.0`**（`:1116`）：

  ```python
  attention_mask.masked_fill_(full_attention_mask, -10000.0)
  ```

  这是为了**避免 fp16 下取极小值导致溢出/NaN**，是边缘设备 fp16 推理的稳定性妥协。
- attention 里从"条件性加 mask"改成**无条件相加**（`:313`），消除动态分支：

  ```python
  attn_weights = attn_weights + attention_mask
  ```

### 4.4 position_ids 强制外部传入

原版会在 `position_ids is None` 时内部生成；魔改版直接：

```python
position_ids = position_ids.view(-1, seq_length).long()  # :1064
```

要求调用方（`kvcache.py` / `engine.py` 的 `get_inputs`）显式算好传入。增量解码时 position 由外部维护。

### 4.5 只保留 eager attention，删除 FlashAttention / SDPA 分支

`Qwen2FlashAttention2` 整段被三引号注释掉（`:338`），因为这些算子在 ONNX/昇腾上不可用。只留最朴素的 matmul-softmax-matmul 版本，保证算子都能被 ATC 识别。

### 4.6 小结

魔改把**动态控制流、动态 shape、HF 专用数据结构**统统替换成静态等价物，同时保持数学计算完全一致，因此能直接加载原始权重。这是整条技术路线中**工作量与风险最集中**的一环。

---

## 5. 技术细节二：PyTorch → ONNX 的转换

使用 **PyTorch 官方自带的 `torch.onnx.export`**（`export/export_onnx.py`），底层是 **TorchScript tracing（追踪）** 机制。

### 5.1 转换流程

1. 用魔改版 `Qwen2ForCausalLM.from_pretrained` 加载权重（NPU/CUDA 用 fp16，CPU 用 fp32）。
2. 构造一组**虚拟输入张量**（dummy inputs）作为 tracing 样本（`:134-145`）：`input_ids`、`attention_mask`、`position_ids`、`past_key_values`（后者用 `torch.rand` 填成目标 KV-cache 形状）。
3. `model.eval()` + `torch.no_grad()`，然后调用导出：

   ```python
   torch.onnx.export(
       model,
       f=onnx_model_path,
       args=input_args,
       input_names=["input_ids", "attention_mask", "position_ids", "past_key_values"],
       output_names=["logits", "out_key_values"],
       dynamic_axes={
           "input_ids":      {0: "batch_size", 1: "seq_length"},
           "attention_mask": {0: "batch_size", 1: "seq_length + kv_len"},
           "position_ids":   {0: "batch_size", 1: "seq_length"},
           "past_key_values":{0: "batch_size", 1: "kv_len"},
       },
       do_constant_folding=False,
       opset_version=14,
       export_params=True,
   )
   ```

### 5.2 关键技术点

- **tracing 机制**：`torch.onnx.export` 会用 dummy input 真正跑一遍前向，记录所有算子调用，生成静态图。**这正是第 4 节要消除所有动态控制流的原因**——tracing 只能记录本次执行走过的路径，`if`/循环会被固化成常量。
- **`dynamic_axes`**：虽然 tracing 固定了形状，但通过声明动态轴，让 batch、seq_length、kv_len 三个维度在 ONNX 图里保持符号化可变，运行时能接受不同长度输入。
- **`opset_version=14`**：选定算子集版本，兼顾算子覆盖与昇腾 ATC 支持度。
- **`do_constant_folding=False`**：关闭常量折叠。因为后面还要对图做手术（改 Trilu 节点），过早折叠会破坏节点结构。
- **`export_params=True`**：把权重一起写进 ONNX。
- 导出前脚本会把 `modeling_qwen2.py` 拷进模型目录并改写 `config.json` 的 `auto_map`，确保加载的是魔改版结构。

导出后可用 `session_type=onnx` 在 CPU 上验证对话是否正常。

---

## 6. 技术细节三：为什么以及如何修改导出的 ONNX

> **原因**：PyTorch 导出的某些 ONNX 算子，昇腾 ATC 编译器无法识别或无法正确处理。因此必须在 ONNX → OM 之前做"图手术"修一遍（`export/change_node.py`，用 `onnx` 官方 Python API）。

脚本遍历图里所有节点，做两类替换：

### 6.1 修复 Trilu 算子（核心问题）

- 第 4 节 `get_masks` 里的 `.tril_()`（下三角）会被导出成 ONNX 的 `Trilu` 算子。PyTorch 导出的 Trilu 带有额外输入（对角线偏移 `k`）和某些属性组合，**昇腾 ATC 无法识别**。
- 修复方式：重建一个"干净"的 Trilu 节点（`change_node.py:50-57`）——只保留第一个输入，去掉多余输入，显式设 `upper=0` 属性（表示保留下三角）：

  ```python
  new_node = helper.make_node(
      "Trilu",
      name="MY_" + node.name,
      inputs=[node.input[0]],
      outputs=node.output,
      upper=0,
  )
  ```

### 6.2 把 INT8 的 Cast 替换成 AscendQuant（为量化预留）

- 遍历中若发现 `Cast` 到 `INT8` 的节点，替换成昇腾专用量化算子 `AscendQuant`（`:58-68`）。当前量化配置为空，这条路径实际未启用，是为将来 weight-only / smooth quant 预留的钩子。

### 6.3 保存

用 `helper.make_graph` / `make_model` 重建整张图，保留原始 `initializer`（权重）、`opset_import`、`ir_version`，最后：

```python
onnx.save(new_model, args.output_model_path, save_as_external_data=True)
```

**权重外置**（`save_as_external_data=True`），绕过 protobuf 单文件 2GB 上限。

---

## 7. 技术细节四：ONNX → OM 的编译

使用昇腾 CANN 的 **ATC（Ascend Tensor Compiler）** 命令行工具完成（`export/onnx2om.py` 本质是拼一条 `atc` 命令并 `subprocess.run`）。

### 7.1 技术路线

ATC 是一个**离线图编译器**：读入 ONNX 图 → 算子映射（映射到昇腾 AI Core 算子库）→ 图融合优化 → 内存规划 → 针对具体芯片型号生成二进制 OM 离线模型。

### 7.2 关键参数

```bash
atc --framework=5                          # 5 = ONNX 输入
    --model=<onnx>  --output=<om>
    --soc_version=Ascend310B1              # 芯片型号，强绑定
    --precision_mode_v2=mixed_float16      # 混合精度模式
    --modify_mixlist=ops_info.json         # 对个别算子做精度覆盖
    --input_format=ND
    --input_shape="input_ids:...;attention_mask:...;position_ids:...;past_key_values:..."
    --dynamic_dims="..."                   # 动态 shape 分档
```

要点：

- **`--framework=5`**：指定输入是 ONNX。
- **`--soc_version`**：脚本用 `ctypes` 调 `libruntime.so` 的 `rtGetSocVersion` **自动探测芯片型号**（也可手动指定）。这是 OM 不可移植的根源——OM 与芯片型号绑死。
- **`--precision_mode_v2=mixed_float16` + `modify_mixlist`**：整体混合精度加速，同时用 `ops_info.json` 白名单让个别对精度敏感的算子保持高精度，避免 fp16 溢出。
- **多线程编译**：通过环境变量 `TE_PARALLEL_COMPILER` / `MAX_COMPILE_CORE_NUMBER` 控制并行度（每线程独立占内存）。

### 7.3 动态 shape 分档（本项目关键优化）

脚本按 `max_prefill_length`（必须是 2 的幂）生成 `[1, 2, 4, ..., max_prefill_length]` 的 prefill 档位，每档再配 kv_cache **半长 / 全长**两种 past_key_values 长度，组合成多档 shape（`onnx2om.py:159-171`）：

```python
for dynamic_kv_cache_length in [kv_cache_length // 2, kv_cache_length]:
    for dynamic_dim in zip(input_ids_length_range, position_length_range):
        new_dynamic_dim = [
            str(dynamic_dim[0]),                            # input_ids
            str(dynamic_dim[0] + dynamic_kv_cache_length),  # attention_mask
            str(dynamic_dim[1]),                            # position_ids
            str(dynamic_kv_cache_length),                   # past_key_values
        ]
        dynamic_dims.append(",".join(new_dynamic_dim))
```

运行时 `AclSession.decompose_number` 把任意输入长度分解成这些档位的组合（如输入 12 → `[8, 4]`）逐段推理，从而在**降低首字延迟**（大 prefill 档一次算多个 token）与**静态编译效率**之间取得平衡。若 `max_prefill_length=1` 则为纯静态图（体积小、内存省，更适合调试）。

产物是一个 `.om` 文件，运行时由 `utils/engine.py` 用 ACL 接口 `acl.mdl.load_from_mem` 加载执行。

---

## 8. 运行时推理：ACL 执行引擎

生产路径 `session_type=acl` 的执行细节（`utils/engine.py` + `utils/session.py`）：

- **资源初始化**（`engine.py:init_resource`）：`acl.init` → `set_device` → `create_context`。
- **模型加载**（`load_model`）：分块（50MB）读文件到 host 内存，再 `acl.mdl.load_from_mem` 加载，随后释放 host buffer 省内存。
- **内存管理**（`allocate_memory`）：为输入/输出在 device 上分配 buffer；其中 `new_kv_cache` 输出**不分配 host 内存**，直接在 device 侧原地更新（省内存省拷贝）。
- **KV-cache 在 device 侧原地更新**（`update_kv_cache`）：用 `acl.rt.memcpy` 的 `DEVICE_TO_DEVICE`，把新算出的 KV 按 fp16（×2 字节）偏移拷进大 cache 张量对应位置，避免搬回 host。
- **动态 shape 派发**（`inference`）：根据当前 `real_kv_size + seq_length` 是否超过 `kv_cache_length // 2`，选择半长或全长档位，用 `set_input_dynamic_dims` 设定本次 shape，再 `acl.mdl.execute` 同步执行。
- **Prefill 优化**：prefill 阶段跳过 logits 回拷（`is_prefill` 时返回 `None`），只有最后一步（decode）才把 logits 拷回 host 做采样，减少无用的 device→host 传输。
- **AclSession 分段调度**（`session.py`）：`decompose_number` 把输入长度分解为 2 的幂之和，逐段调 `run_some`，配合 OM 动态档位实现"用有限档位覆盖任意长度输入"。

上层服务：`utils/inference.py` 统一三种后端的分词、采样（greedy / top_p / top_k）、流式输出、停止符处理；`cli_chat.py` 提供交互式对话；`api.py` 提供 OpenAI 兼容接口。

### 8.1 调试与质量保障（compare.py + MSIT）

当 OM（fp16）输出异常而 ONNX 正常时，用昇腾 MSIT 工具逐层对比 ONNX 与 OM 的输入输出，定位从哪一层开始溢出/偏差过大。建议用最小的 Qwen-0.5B、静态图（`max_prefill_length=1`）来对比，节省时间、便于分析。这正是 ONNX 作为"黄金参照"价值的体现。

---

## 9. 关键参数约束

这条流水线能跑通的前提是几个参数在导出、编译、运行时严格对齐：

- **`kv_cache_length` 全程一致**：导出 ONNX、编译 OM、运行时 `max_output_length` 必须相等，否则输出异常或编译失败。在 `cli_chat.py` 中 `kv_cache_length = args.max_output_length`，因此 `max_output_length` 实质上就是**上下文窗口大小**，同时决定 KV-cache 张量的物理长度。
- **长度规则**：`max_input_length < kv_cache_length`（通常取一半）；`max_output_length = kv_cache_length`；实际最大生成 token 数 = `max_output_length − min(max_input_length, 实际输入 token 数)`。
- **`max_prefill_length`**：编译与运行时必须相同，是 2 的幂；越大首字延迟越低，但 OM 体积（+50%~100%）和内存占用越高。
- **dtype**：NPU/CUDA 导出 fp16、CPU 导出 fp32、ACL 恒为 fp16。

> **关于 `max_output_length` 越大越慢的原因**：由于 KV-cache 被做成满长度固定张量，attention 计算和内存搬运都按 `kv_cache_length` 满长度进行（靠 mask 屏蔽无效位，但算力和带宽照付）。因此这个值越大，每一步 decode 的计算量、内存搬运量、内存占用都成比例上升，单 token 延迟变大、decode 速度下降。实践中应把 `max_output_length` 设成够用的最小值。

---

## 10. 迁移到其他模型（Qwen3 / DeepSeek）的通用性分析

> 注：CLI 里 `MODEL_NAME = "DeepSeek-R1-Distill-Qwen-1.5B"` 说明作者已在跑 Qwen 架构蒸馏的 DeepSeek——它本质仍是 Qwen2 结构。

### 10.1 基本通用、几乎不用改的步骤

| 步骤 | 说明 |
|---|---|
| **整体技术路线** | PyTorch→ONNX→OM 三段式框架完全通用 |
| **导出工具链** | `torch.onnx.export`、`change_node.py`、`atc` 命令的调用方式通用 |
| **change_node.py 的 Trilu 修复** | 只要模型用因果掩码就都需要，且是模型无关的图手术，通用 |
| **onnx2om.py 的 ATC 编译逻辑** | 参数拼接、soc 探测、动态分档逻辑通用（shape 中的层数/头数自动从 config 读取） |
| **运行时（engine/session/inference/kvcache）** | KV-cache 布局、动态 shape 派发、采样、流式——只要 KV-cache 形状约定不变就通用 |
| **config.py** | 通过 `Qwen2Config` 自动读 `num_hidden_layers` / `num_key_value_heads` / `hidden_size`，参数化程度高 |

### 10.2 需要修改 / 人为调试的步骤

**(1) `modeling_xxx.py` —— 最大的工作量，必须为新架构重写魔改版**

- 本项目的 `modeling_qwen2.py` 是**针对 Qwen2 架构手工魔改**的，硬编码了 KV-cache 切片索引方式、`get_masks` 构造、RoPE 实现、attention 的具体算子。
- **换 Qwen3**：Qwen3 相比 Qwen2 有结构差异（如 **QK-Norm** 等）。需拿 Qwen3 原版 `modeling_qwen3.py`，**照同样思路重新魔改一遍**（消除动态控制流、固定 KV-cache 形状、替换 mask 构造），不能直接复用。
- **换原生 DeepSeek（如 V2/V3）**：架构差异更大——**MLA（Multi-head Latent Attention）** 的 KV-cache 是压缩潜向量，与本项目 `(batch, len, layers*2*kv_heads, head_dim)` 布局**完全不兼容**；还有 **MoE** 路由（动态专家选择，是动态控制流，ONNX 导出困难）。几乎需要重写整个 KV-cache 系统和 modeling，是重度改造。
- 但 **DeepSeek-R1-Distill-Qwen 系列**是 Qwen 架构，可直接复用当前 `modeling_qwen2.py`（作者正是这么做的）。

**(2) tokenizer / chat template**

- `inference.py` 用 `apply_chat_template` 和 `eos_token_id`。不同模型的对话模板、特殊 token、停止符不同，需确认新模型 template 正确（尤其 DeepSeek-R1 有 `<think>` 推理标签，可能需要额外处理）。

**(3) fp16 精度调试（大概率需要人工介入）**

- 新模型转 fp16 OM 后，可能出现某些层数值溢出导致输出乱码。需用 `export/compare.py` + MSIT 工具**逐层对比 ONNX 与 OM**，找出溢出层，然后在 `ops_info.json` 里把该层加入高精度白名单。这是**每个新模型都可能要重新调**的经验性工作。

**(4) 参数配置**

- `kv_cache_length`、`max_prefill_length` 需按新模型的 `max_position_embeddings` 和内存预算重设；`soc_version` 按目标芯片设。配置层面，改动简单。

### 10.3 迁移难度总结

| 换成 | 难度 | 关键工作 |
|---|---|---|
| **DeepSeek-R1-Distill-Qwen** | 低 | 几乎直接可用（Qwen 架构），调 tokenizer/template + fp16 精度 |
| **Qwen3** | 中 | 重写 `modeling_qwen3.py` 魔改版（处理 QK-Norm 等差异）+ fp16 调试 |
| **原生 DeepSeek-V2/V3（MLA+MoE）** | 高 | 重写 KV-cache 系统 + 解决 MoE 动态路由的 ONNX 导出 + modeling 深度改造 |

> **核心规律**：越靠近"运行时/工具链"的部分越通用（框架无关），越靠近"模型定义（modeling + KV-cache 布局 + 精度）"的部分越需要针对具体架构人工改造和调试。真正的工作量与风险都集中在 `modeling_xxx.py` 的重写和 fp16 逐层精度调试上。

---

## 11. 总结

本项目用一条清晰的 **PyTorch → ONNX → OM** 三段式路线解决了"通用大模型如何落到昇腾边缘 NPU"的工程问题：

- **PyTorch** 提供权重与结构验证基准（并针对导出做了 KV-cache 静态化、mask/position 外置的结构魔改）；
- **ONNX** 作为跨框架桥梁与 CPU 可运行的黄金参照，承担验证、改图、逐层对比调试；
- **OM** 作为昇腾专用离线模型，经 ATC 深度编译优化，配合动态 shape 分档和 device 侧 KV-cache 原地更新，在 310B1 这类边缘芯片上实现低首字延迟的生产推理。

ONNX 胜在**通用与可调试**、OM 胜在**硬件极致优化**，二者恰好互补：用 ONNX 保证正确性和可移植性，用 OM 保证边缘设备上的性能。项目在此之上补齐了流式、OpenAI 兼容 API、function call、Docker 部署等工程能力，形成了一套从模型转换到服务落地的完整边缘大模型部署方案。

整条路线中，**技术含量与工程风险最高的三处**是：

1. **模型结构魔改**（`modeling_qwen2.py`）：把动态图改造为可静态导出的等价形式，同时保持数学一致；
2. **ONNX 图手术 + ATC 动态分档编译**：解决算子兼容性并兼顾首字延迟；
3. **fp16 逐层精度调试**：借助 ONNX 黄金参照 + MSIT 定位溢出层。

这也正是将该方案迁移到 Qwen3、DeepSeek 等新模型时最需要投入人力的地方。
